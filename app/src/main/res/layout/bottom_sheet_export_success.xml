<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@android:color/white"
    android:paddingTop="16dp"
    android:paddingBottom="8dp">

    <!-- Header with icon and title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="16dp">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="12dp"
            android:src="@drawable/ic_check_circle"
            app:tint="@color/green" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Export Successful"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@android:color/black" />

        <ImageView
            android:id="@+id/btnCloseHeader"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_close"
            android:tint="@color/gray_600"
            android:padding="4dp"
            android:background="?selectableItemBackgroundBorderless" />

    </LinearLayout>

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/gray_200"
        android:layout_marginBottom="16dp" />

    <!-- File Info Card -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_status_badge"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="24dp"
        android:padding="16dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Exported File"
            android:textSize="12sp"
            android:textColor="@color/gray_600"
            android:textAllCaps="true"
            android:letterSpacing="0.1"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/tvFileName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="File_Name.xlsx"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            android:layout_marginBottom="4dp"
            tools:text="Attendance_Report_20240109_143022.xlsx" />

        <TextView
            android:id="@+id/tvFilePath"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="/storage/emulated/0/Download/AttendSmart"
            android:textSize="12sp"
            android:textColor="@color/dark_gray"
            android:maxLines="2"
            android:ellipsize="end"
            tools:text="/storage/emulated/0/Download/AttendSmart/Attendance_Report_20240109_143022.xlsx" />

    </LinearLayout>

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="8dp">

        <!-- View Excel Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnViewExcel"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginBottom="12dp"
            android:text="Open in Excel"
            android:textSize="14sp"
            android:letterSpacing="0.1"
            app:icon="@drawable/ic_excel"
            app:iconGravity="start"
            app:iconPadding="12dp"
            app:iconSize="20dp"
            app:iconTint="@color/gray_600"
            style="@style/Widget.Material3.Button.ElevatedButton" />

        <!-- Share Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnShare"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginBottom="12dp"
            android:text="Share File"
            android:textSize="14sp"
            android:letterSpacing="0.1"
            app:icon="@drawable/ic_share"
            app:iconGravity="start"
            app:iconPadding="12dp"
            app:iconSize="20dp"
            style="@style/Widget.Material3.Button.OutlinedButton" />

        <!-- Copy Path Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCopyPath"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginBottom="12dp"
            android:text="Copy File Path"
            android:textSize="14sp"
            android:letterSpacing="0.1"
            app:icon="@drawable/ic_copy"
            app:iconGravity="start"
            app:iconPadding="12dp"
            app:iconSize="20dp"
            style="@style/Widget.Material3.Button.OutlinedButton" />

        <!-- Close Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnClose"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Close"
            android:textSize="14sp"
            android:letterSpacing="0.1"
            style="@style/Widget.Material3.Button.TextButton" />

    </LinearLayout>

</LinearLayout>